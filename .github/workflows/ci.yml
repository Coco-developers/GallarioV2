name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Build 
        uses: actions/checkout@v5
      - run: echo "Docker image building"
      - run: docker build -t gallario .